@page
@using _0_Framework.Application
@using Microsoft.AspNetCore.Mvc.TagHelpers
@model ServiceHost.Areas.Administration.Pages.Shop.Order.ItemsModel
@{
}


<div class="modal-header">
    
    <h4 class="modal-title">محصولات م.وجود در سفارش </h4>
</div>
@*@if (Model.SuccessMessageame != null)
{
    <div class="alert alert-success">
        @Model.SuccessMessageame

    </div>
}
@if (Model.ErrorMessageame != null)
{
    <div class="alert alert-danger">
        @Model.ErrorMessageame

    </div>
}
*@
<table class="table table-striped table-bordered">
    <thead>
    <tr>
    <th>#</th>
    <th>محصول</th>
    <th>تعداد</th>
    <th>قیمت واحد</th>
    <th>درصد تخفیف</th>
    </thead>
    <tbody>
    @foreach (var item in Model.Command)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Product</td>
            <td>@item.Count</td>
            <td>@item.UnitPrice.ToMoney()</td>
            <td>@item.DiscountRate %</td>
        </tr>
    }
    </tbody>
   
   
</table>

<div>
    <a class="btn btn-warning  waves-effect waves-light pull-right m-rl-5">
        <a  asp-page="./Index" > بازگشت </a>
    </a>
</div>


